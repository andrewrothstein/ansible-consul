---
consul_zip : 'consul_{{consul_ver}}_{{consul_platform}}.zip'
consul_url : '{{consul_mirror}}/{{consul_ver}}/{{consul_zip}}'
consul_agent_bind : "{{hostvars[inventory_hostname]['ansible_' + consul_agent_network_iface]['ipv4']['address']}}"
consul_agent_client_bind : '{{consul_agent_bind}}'